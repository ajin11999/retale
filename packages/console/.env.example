# GraphQL endpoint the console server talks to: server-side requests (login,
# auth guard), the /graphql proxy that browser requests flow through, and
# Houdini codegen (`houdini generate`).
GRAPHQL_URL=http://localhost:3000/graphql
